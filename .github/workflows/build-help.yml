name: Build Help

on:
  push:
    branches:
      - master

jobs:

  help:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Help
        run: |
          chmod +x configure
          ./configure --help
